import { type ContentType } from '@/types';

/**
 * @description
 * Uses a list of titles to generate matching (directory) files in a vault.
 */
export async function createFromList(contentType: ContentType) {}
